import React from 'react';
import { Typography } from '@mui/material';

function FileManager() {
  return (
    <div>
      <Typography variant="h4" gutterBottom>
        File Manager
      </Typography>
      <Typography paragraph>
        This is the FileManager page.
      </Typography>
    </div>
  );
}

export default FileManager;